#include "main.h"
#include<stdio.h>
/**
 * main - check if a character is uppercase.
 * Return:1 if c is uppercase and 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 'A'&&c <= 'Z')
	
	return (1);

	else

	return(0);
}
